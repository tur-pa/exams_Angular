import { FilterMovementPipe } from './filter-movement.pipe';

describe('FilterMovementPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMovementPipe();
    expect(pipe).toBeTruthy();
  });
});
