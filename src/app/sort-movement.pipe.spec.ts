import { SortMovementPipe } from './sort-movement.pipe';

describe('SortMovementPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMovementPipe();
    expect(pipe).toBeTruthy();
  });
});
