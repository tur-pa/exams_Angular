import { SortByHighScoresPipe } from './sort-by-high-scores.pipe';

describe('SortByHighScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByHighScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
